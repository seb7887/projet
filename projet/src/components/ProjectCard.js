import React from "react";


const ProjectCard = props => {
  const { name, idea, features, keywords } = props;
  return (
    <div className={cardStyles.modal}>
      
    </div>
  );
};

export default ProjectCard;
